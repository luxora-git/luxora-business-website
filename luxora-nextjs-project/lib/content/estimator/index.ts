import type { EstimatorContent } from './types';

export * from './types';

/**
 * Phase 1 scaffold — intentionally empty. Real content (categories, style
 * taxonomy, package tiers, pricing) is authored in a later phase per the
 * approved Implementation Plan §6 and Component Reuse Matrix §J.
 */
export const estimatorContent: EstimatorContent = {
  categories: [],
  styles: [],
  packageTiers: [],
};
