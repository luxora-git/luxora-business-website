import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * StyleScreen — Phase 1 placeholder only.
 * TODO (later phase): "Visual Inspiration" style-discovery screen per UI Spec v1.0, Screen 3.
 */
export default function StyleScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Style" />
    </EstimatorLayout>
  );
}
