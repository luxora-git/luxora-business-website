import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * ResumeScreen — Phase 1 placeholder only.
 * TODO (later phase): "Welcome back" resume screen per UI Spec v1.0, Alternate Screen — Resume. No session-persistence logic yet.
 */
export default function ResumeScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Resume" />
    </EstimatorLayout>
  );
}
