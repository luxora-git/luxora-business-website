import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * QuestionsScreen — Phase 1 placeholder only.
 * TODO (later phase): Personalisation steps (BHK/size/rooms or shape/finish) per UI Spec v1.0, Screen 4.
 */
export default function QuestionsScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Questions" />
    </EstimatorLayout>
  );
}
