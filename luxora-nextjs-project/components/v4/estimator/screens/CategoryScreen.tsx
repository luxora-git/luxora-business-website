import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * CategoryScreen — Phase 1 placeholder only.
 * TODO (later phase): "Choose Your Project" screen per UI Spec v1.0, Screen 2 (Choose Your Project).
 */
export default function CategoryScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Category" />
    </EstimatorLayout>
  );
}
