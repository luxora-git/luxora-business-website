import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * ProposalScreen — Phase 1 placeholder only.
 * TODO (later phase): Design Proposal screen per UI Spec v1.0, Screen 10 — full chrome (V4FooterSection) restores here per the approved spec.
 */
export default function ProposalScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Proposal" />
    </EstimatorLayout>
  );
}
