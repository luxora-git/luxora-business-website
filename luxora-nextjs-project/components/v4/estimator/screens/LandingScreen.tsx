import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * LandingScreen — Phase 1 placeholder only.
 * TODO (later phase): full cinematic entry screen per UI Spec v1.0, Screen 1 (Landing).
 */
export default function LandingScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Landing" />
    </EstimatorLayout>
  );
}
