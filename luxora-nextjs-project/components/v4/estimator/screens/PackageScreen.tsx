import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * PackageScreen — Phase 1 placeholder only.
 * TODO (later phase): Package Selection screen per UI Spec v1.0, Screen 7 — see EstimatorPackageCard in the Component Reuse Matrix §D.
 */
export default function PackageScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Package" />
    </EstimatorLayout>
  );
}
