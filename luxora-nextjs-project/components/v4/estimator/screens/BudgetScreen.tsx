import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * BudgetScreen — Phase 1 placeholder only.
 * TODO (later phase): Live Budget Intro screen per UI Spec v1.0, Screen 5 — no pricing logic yet.
 */
export default function BudgetScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Budget" />
    </EstimatorLayout>
  );
}
