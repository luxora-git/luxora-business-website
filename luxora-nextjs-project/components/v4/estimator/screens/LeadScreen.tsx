import V4SectionHeader from '@/components/v4/V4SectionHeader';
import EstimatorLayout from '../EstimatorLayout';

/**
 * LeadScreen — Phase 1 placeholder only.
 * TODO (later phase): Lead Capture screen per UI Spec v1.0, Screen 9 — no form fields or submission logic yet.
 */
export default function LeadScreen() {
  return (
    <EstimatorLayout>
      <V4SectionHeader eyebrow="Luxora Estimator" title="Lead" />
    </EstimatorLayout>
  );
}
